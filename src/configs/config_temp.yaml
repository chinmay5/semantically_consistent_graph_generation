defaults:
    - _self_
    - general : general_default
    - model : discrete_vessap
    - train : train_default
    - dataset : atm

hydra:
  job:
    chdir: True
#  run:
#    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}-${dataset.name}-radius_${dataset.use_radius}-equivariant_${model.equivariant}-transition_${model.transition}




